bytes in memory, depends on OS, application, the hardware running,...
need a way to transmit that data from one system to another

bind it to a syntactical format that can be parsed again by another system
